# Datos procesados

Esta carpeta contiene las salidas generadas por el preprocesamiento. La salida
canónica esperada por los algoritmos es:

```text
lapop_bolivia_2023_processed.csv
```

No agregues estos archivos a Git: se regeneran localmente desde los datos de
`data/raw/` mediante el flujo de Iteración 1.
