# Datos crudos

Coloca aquí el archivo original de LAPOP Bolivia 2023 obtenido por la vía
documentada en los notebooks de Iteración 1:

```text
lapop_bolivia_2023.dta
```

Los datos crudos no se versionan. Ejecuta el preprocesamiento desde la
interfaz o los notebooks para generar la salida correspondiente en
`data/processed/`.
